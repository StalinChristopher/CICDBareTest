/**
 * User-visible app name in chrome (e.g. top bar). Replaced when scaffolding: CICDBareTest → MyApp.
 * Keep aligned with `app.json` `displayName` / native display name.
 */
export const APP_DISPLAY_NAME = 'CICDBareTest';
